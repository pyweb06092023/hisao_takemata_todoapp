<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todoapp.TodoMapper">
    <select id="search" resultType="com.example.todoapp.Todo">
        SELECT
            id,
            title,
            detail,
            category,
            priority,
            due_date,
            completed,
            created_at,
            updated_at
        FROM todos
        <where>
            <if test="keyword != null and keyword != ''">
                title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="category != null and category != '' and category != 'すべて'">
                AND category = #{category}
            </if>
            <if test="from != null">
                AND due_date IS NOT NULL AND due_date &gt;= #{from}
            </if>
            <if test="to != null">
                AND due_date IS NOT NULL AND due_date &lt;= #{to}
            </if>
        </where>
        <choose>
            <when test="order == 'desc'">
                ORDER BY due_date DESC
            </when>
            <otherwise>
                ORDER BY due_date ASC
            </otherwise>
        </choose>
    </select>

    <select id="searchForList" resultType="com.example.todoapp.Todo">
        SELECT id, title, detail, category, priority, due_date, completed, created_at, updated_at
        FROM todos
        <where>
            <if test="keyword != null and keyword != ''">title LIKE CONCAT('%', #{keyword}, '%')</if>
            <if test="category != null and category != '' and category != 'すべて'">AND category = #{category}</if>
            <if test="includeCompleted == false">AND completed = FALSE</if>
        </where>
        <choose>
            <when test="order == 'desc'">ORDER BY due_date DESC</when>
            <otherwise>ORDER BY due_date ASC</otherwise>
        </choose>
    </select>

    <select id="findById" parameterType="long" resultType="com.example.todoapp.Todo">
        SELECT
            id,
            title,
            detail,
            category,
            priority,
            due_date,
            completed,
            created_at,
            updated_at
        FROM todos
        WHERE id = #{id}
    </select>

    <delete id="deleteById" parameterType="long">
        DELETE FROM todos
        WHERE id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.todoapp.Todo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todos (
            title,
            detail,
            category,
            priority,
            due_date,
            completed
        )
        VALUES (
            #{title},
            #{detail},
            #{category},
            #{priority},
            #{dueDate},
            COALESCE(#{completed}, FALSE)
        )
    </insert>

    <update id="update" parameterType="com.example.todoapp.Todo">
        UPDATE todos
        SET
            title = #{title},
            detail = #{detail},
            category = #{category},
            priority = #{priority},
            due_date = #{dueDate},
            completed = #{completed}
        WHERE id = #{id}
    </update>
</mapper>
