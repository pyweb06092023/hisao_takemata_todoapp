# 要件定義書

## 1. 文書の目的

この文書は、`handout_app_spec.md` をもとに、Todoアプリの要件（作るうえで満たすべき内容）を整理したものです。

本書では、叩き台に書かれている内容を中心にまとめています。
叩き台にない補助説明を足した箇所は、各見出しの中で **【補足】** と明記します。

## 2. 対象システムの概要

本システムは、やることを登録して管理するTodoアプリです。

利用者は1人を想定し、ログイン機能は設けません。

### 2.1 システムの目的

- やることを登録できること
- 登録したやることを一覧で確認できること
- 登録内容を編集できること
- 不要になったやることを削除できること
- やることを検索できること
- 期限順に並び替えできること

## 3. 適用範囲

この文書で定義する対象は、Todoアプリの要件定義のみです。
DB設計書（データベースの設計書）と画面設計書（画面ごとの詳しい設計書）は対象外とします。

## 4. 利用者

- 利用者は本人1名です
- 複数ユーザー利用は対象外です
- ログイン機能は不要です

## 5. 業務要件

### 5.1 実現する機能

| No. | 機能名 | 内容 |
|---|---|---|
| 1 | 一覧機能 | 登録したやることを1画面に一覧表示する |
| 2 | 登録機能 | 新しいやることを1件登録する |
| 3 | 編集機能 | 登録済みのやることを修正する |
| 4 | 削除機能 | 不要なやることを削除する |
| 5 | 検索機能 | やること名やジャンルで一覧を絞り込む |
| 6 | 並び替え機能 | 期限の順で一覧を並び替える |

### 5.2 対象データ

管理対象は「やること」1件ごとの情報です。
保存先はMySQL内の `todos` テーブル1つです。

## 6. 機能要件

### 6.1 一覧機能

- URLは `/todos` とする
- 登録済みのやることを一覧表示する
- 表示項目は、やること、ジャンル、優先度、期限、状態、操作とする
- 画面上部に、検索欄、ジャンル選択欄、絞り込みボタン、並び替えリンク、新規登録ボタンを配置する
- 優先度は保存値 `1` `2` `3` を、画面上では「高」「中」「低」と表示する
- 完了状態は保存値 `1` `0` を、画面上では「完了」「未完了」と表示する
- データが1件もない場合は「まだ1件も登録されていません」と表示する
- 直前の操作結果を画面上部に1行表示する

### 6.2 登録機能

- URLは `/todos/new` とする
- 新規のやることを入力して確認画面へ進める
- 入力項目は、やること、メモ、ジャンル、優先度、期限とする
- 必須項目は、やること、ジャンル、優先度とする

### 6.3 登録確認機能

- URLは `/todos/confirm` とする
- 入力された5項目を確認表示する
- 未入力の任意項目（必須ではない項目）は「（入力なし）」と表示する
- 「登録する」で保存し、「書き直す」で入力画面へ戻れること

### 6.4 編集機能

- URLは `/todos/{id}/edit` とする
- 指定した `id` のやることを編集できること
- 入力項目は、登録時の5項目に加えて完了を含む6項目とする
- 初期表示時に現在の値を表示する
- 必須項目は、やること、ジャンル、優先度とする

### 6.5 編集確認機能

- URLは `/todos/{id}/confirm` とする
- 編集内容を確認表示する
- 「保存する」で更新し、「書き直す」で編集画面へ戻れること

### 6.6 削除確認機能

- URLは `/todos/{id}/delete` とする
- 削除対象のやること、ジャンル、期限を表示する
- 入力欄は設けない
- 「消す」で削除し、「やめる」で一覧へ戻れること

### 6.7 検索機能

- やること名で絞り込みできること
- ジャンルで絞り込みできること
- やること名は `title` に入力語が一部でも含まれる場合に一致とみなす
- ジャンルは `category` が完全一致した場合に一致とみなす
- ジャンルで「すべて」を選んだ場合は絞り込みを行わない
- 条件未指定時は全件表示する
- 結果が0件の場合も「まだ1件も登録されていません」と表示する
- 絞り込み条件はURLのクエリパラメータ（URLの末尾に付く条件文字列）として保持する

### 6.8 並び替え機能

- `due_date` を基準に並び替える
- 初期表示時は期限が近い順とする
- 期限が近い順と期限が遠い順の2種類を提供する
- 並び順はURLのクエリパラメータとして保持する

### 6.9 画面遷移要件

- トップページ `/` を用意する
- 一覧から登録、編集、削除確認へ遷移できること
- 登録と編集は確認画面を経由すること
- 登録確認と編集確認では「書き直す」で前画面へ戻れること
- 登録画面と編集画面からは「一覧へ」で一覧へ戻れること
- 削除確認画面からは「やめる」で一覧へ戻れること

## 7. データ要件

### 7.1 テーブル要件

保存先テーブルは `todos` とします。

| 列名 | 内容 | 型 | 必須 | 備考 |
|---|---|---|---|---|
| `id` | 1件を識別する番号 | `BIGINT` | 必須 | 自動採番、主キー（データを一意に見分ける列） |
| `title` | やること | `VARCHAR(255)` | 必須 |  |
| `detail` | メモ | `VARCHAR(255)` | 任意 |  |
| `category` | ジャンル | `VARCHAR(255)` | 必須 |  |
| `priority` | 優先度 | `INT` | 必須 | 既定値は `2` |
| `due_date` | 期限 | `DATE` | 任意 |  |
| `completed` | 完了状態 | `BOOLEAN` | 必須 | 既定値は未完了 |
| `created_at` | 登録日時 | `DATETIME` | 必須 | MySQLが自動設定 |
| `updated_at` | 更新日時 | `DATETIME` | 必須 | MySQLが自動設定 |

### 7.2 ジャンル要件

`category` に設定できる値は、以下の5つに限定します。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

入力方式は自由入力ではなく選択式とします。

### 7.3 自動設定項目

以下の項目は、利用者が直接入力しません。

- `id`
- `created_at`
- `updated_at`

## 8. 入力要件

### 8.1 入力ルール

| 項目 | ルール | エラーメッセージ |
|---|---|---|
| やること | 未入力不可 | やることを入力してください |
| やること | 255文字以内 | やることは255文字以内で入力してください |
| メモ | 255文字以内 | メモは255文字以内で入力してください |
| ジャンル | 未選択不可 | ジャンルを選んでください |
| 優先度 | 未選択不可 | 優先度を選んでください |

- 期限には入力制約を設けない
- 半角空白だけ、または全角空白だけの入力も未入力として扱う
- 入力エラー時は保存せず、入力画面に留まる
- エラーメッセージは該当項目の下に表示する

### 8.2 完了項目

- 完了項目は編集画面でのみ扱う
- 入力方式はチェックボックス（チェックを入れて選ぶ欄）とする

## 9. 例外要件

- 存在しない `id` で編集画面または削除確認画面を開いた場合は一覧画面へ戻す
- その際、一覧画面上部に「見つかりませんでした」と表示する
- 専用のエラー画面は作成しない

## 10. メッセージ要件

一覧画面上部に表示するメッセージは以下のとおりです。

| 条件 | メッセージ |
|---|---|
| 登録完了後 | 登録しました |
| 編集保存後 | 保存しました |
| 削除完了後 | 削除しました |
| 存在しない番号指定時 | 見つかりませんでした |

## 11. ログ要件

- 登録、編集、削除が実行された際に1行ずつ記録を残す
- 記録対象は、操作種別（登録、編集、削除）と対象 `id` とする
- `title` と `detail` の内容は記録しない

## 12. 画面要件

### 12.1 画面一覧

| 画面名 | URL | 用途 |
|---|---|---|
| トップページ | `/` | 既存のトップページを利用する |
| 一覧 | `/todos` | 登録済みデータの確認、検索、並び替え、各操作の起点 |
| 登録 | `/todos/new` | 新規データ入力 |
| 登録確認 | `/todos/confirm` | 登録前の確認 |
| 編集 | `/todos/{id}/edit` | 既存データ修正 |
| 編集確認 | `/todos/{id}/confirm` | 更新前の確認 |
| 削除確認 | `/todos/{id}/delete` | 削除前の確認 |

### 12.2 画面共通要件

- 見た目はCSSファイル1枚を全画面で共通利用する
- 画面ごとに別CSSは作成しない
- 外部CSS（外から読み込むCSS）は使用しない
- 影、動き、画像による装飾は追加しない

## 13. 画面デザイン要件

### 13.1 CSS配置

- CSSファイルは `src/main/resources/static/css/style.css` とする
- 各テンプレートの `<head>` に `<link rel="stylesheet" href="/css/style.css">` を記述する
- 画面内に `style="..."` を直接書かない

### 13.2 使用class

使用するclass名は以下に限定します。

| 用途 | class名 |
|---|---|
| 画面全体の入れ物 | `container` |
| 画面タイトル | `page-title` |
| 一覧表 | `todo-table` |
| 操作結果メッセージ | `flash` |
| 絞り込みと並び替えの帯 | `search-bar` |
| 入力欄1行 | `form-row` |
| ラベル | `form-label` |
| 入力欄・選択欄 | `form-input` |
| エラーメッセージ | `error` |
| 確認画面の値 | `confirm-value` |
| 確認画面のボタン並び | `confirm-actions` |
| ボタン | `btn` |
| 補助ボタン | `btn sub` |
| 文字リンク | `link` |

### 13.3 CSS内容

`style.css` の具体的な記述内容は `handout_app_spec.md` に従います。

**【補足】**
この要件定義書では、CSSのコード全文は再掲せず、叩き台を正とします。
これは要件定義書を読みやすくするための整理であり、見た目の要件自体を変えるものではありません。

## 14. 非機能要件

### 14.1 開発上の制約

- 今回作る機能は、叩き台に記載された範囲に限定する
- 記載のない機能は追加しない

### 14.2 保守上の制約

**【補足】**
叩き台に「この仕様書に書いていないことは、今回は作らない」とあるため、本書でも同じ方針を要件として扱います。

## 15. 今回の開発対象外

- 一覧のページ分け
- 削除データの復元
- 完了日時の記録
- ログイン機能
- 複数人での共有
- 写真やファイルの添付

## 16. 叩き台からの補足記載一覧

本書で叩き台にない補助説明として追加したのは、以下です。

| 見出し | 追加内容 |
|---|---|
| 1. 文書の目的 | 要件定義書としての位置づけ説明 |
| 3. 適用範囲 | 今回の文書対象が要件定義のみであることの明記 |
| 13.3 CSS内容 | CSS全文を再掲しない整理方針の明記 |
| 14.2 保守上の制約 | 「書いていないことは作らない」を運用方針として整理 |
