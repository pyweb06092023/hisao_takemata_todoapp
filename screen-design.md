# 画面設計書

## 1. この設計書の目的

この設計書は、`handout_app_spec.md`・`requirements.md`・`db-design.md` をもとに、Todoアプリの画面ごとの作りをまとめたものです。

画面設計書（がめんせっけいしょ: 画面に何を出し、どう動くかを決める資料）として、次の内容を整理します。

- 画面一覧
- 各画面に出す項目
- 画面に出す言葉
- 画面遷移（がめんせんい: どの画面からどの画面へ移るか）
- 入力エラー時の見せ方

## 2. 前提と方針

- 作る画面は 6 画面です。増やしません。
- トップページ `/` は既存画面を使うため、本書の詳細設計対象には含めません。
- 利用者は 1 人で、ログイン画面はありません。
- 見た目は全画面で共通の CSS 1 枚を使います。
- `id`・`created_at`・`updated_at` は画面入力に出しません。

### 2.1 ジャンル名の扱い

元資料のうち、`db-design.md` のジャンル名だけ一部が他 2 つと異なっていました。
画面に出す言葉は、`handout_app_spec.md` と `requirements.md` で共通している次の 5 つを使います。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

## 3. 画面一覧

| No. | 画面名 | URL | 役割 |
|---|---|---|---|
| 1 | 一覧画面 | `/todos` | やることの一覧表示、検索、並び替え、編集・削除への入口 |
| 2 | 登録画面 | `/todos/new` | 新しいやることを入力する |
| 3 | 登録確認画面 | `/todos/confirm` | 登録前に入力内容を確認する |
| 4 | 編集画面 | `/todos/{id}/edit` | 既存データを修正する |
| 5 | 編集確認画面 | `/todos/{id}/confirm` | 保存前に修正内容を確認する |
| 6 | 削除確認画面 | `/todos/{id}/delete` | 削除対象を確認し、削除するか決める |

## 4. 画面遷移

画面遷移（がめんせんい: 画面から画面へ移る流れ）は次のとおりです。

| 移動元 | 操作 | 移動先 |
|---|---|---|
| 一覧画面 | `新規登録` ボタン | 登録画面 |
| 登録画面 | `確認する` ボタン | 登録確認画面 |
| 登録画面 | `一覧へ` ボタン | 一覧画面 |
| 登録確認画面 | `登録する` ボタン | 一覧画面 |
| 登録確認画面 | `書き直す` ボタン | 登録画面 |
| 一覧画面 | 各行の `編集` リンク | 編集画面 |
| 編集画面 | `確認する` ボタン | 編集確認画面 |
| 編集画面 | `一覧へ` ボタン | 一覧画面 |
| 編集確認画面 | `保存する` ボタン | 一覧画面 |
| 編集確認画面 | `書き直す` ボタン | 編集画面 |
| 一覧画面 | 各行の `削除` リンク | 削除確認画面 |
| 削除確認画面 | `消す` ボタン | 一覧画面 |
| 削除確認画面 | `やめる` ボタン | 一覧画面 |

### 4.1 同じ画面内での動き

- 一覧画面で `絞り込む` を押すと、一覧画面のまま条件を反映します。
- 一覧画面で `期限が近い順` または `期限が遠い順` を押すと、一覧画面のまま並び順を変えます。

## 5. 共通表示ルール

### 5.1 画面タイトル

各画面の見出し（みだし: 画面の大きいタイトル）は次の文言とします。

| 画面名 | 画面タイトル |
|---|---|
| 一覧画面 | Todo一覧 |
| 登録画面 | Todo登録 |
| 登録確認画面 | Todo登録確認 |
| 編集画面 | Todo編集 |
| 編集確認画面 | Todo編集確認 |
| 削除確認画面 | Todo削除確認 |

### 5.2 表示値の変換

保存値（ほぞんち: DBに入る値）と画面表示は次の対応にします。

| 項目 | 保存値 | 画面に出す言葉 |
|---|---|---|
| 優先度 | `1` | 高 |
| 優先度 | `2` | 中 |
| 優先度 | `3` | 低 |
| 状態 | `TRUE` / `1` | 完了 |
| 状態 | `FALSE` / `0` | 未完了 |

### 5.3 未入力の見せ方

- 確認画面で、メモまたは期限が未入力のときは `（入力なし）` と表示します。
- 一覧画面で期限が未入力のときも `（入力なし）` と表示します。

### 5.4 完了メッセージ

一覧画面の上部に出すメッセージ（操作結果を伝える文）は次のとおりです。

- 登録後: `登録しました`
- 編集保存後: `保存しました`
- 削除後: `削除しました`
- 存在しない番号を指定したとき: `見つかりませんでした`

## 6. 一覧画面

### 6.1 基本情報

| 項目 | 内容 |
|---|---|
| 画面名 | 一覧画面 |
| URL | `/todos` |
| 役割 | 登録済みのやることを一覧表示し、検索・並び替え・登録・編集・削除へ進む |

### 6.2 画面に出す項目

#### 上部

- 画面タイトル: `Todo一覧`
- 操作結果メッセージ: `登録しました` / `保存しました` / `削除しました` / `見つかりませんでした`
- キーワード入力欄のラベル: `やること名`
- ジャンル選択欄のラベル: `ジャンル`
- 絞り込みボタン: `絞り込む`
- 並び替えリンク: `期限が近い順`
- 並び替えリンク: `期限が遠い順`
- 新規登録ボタン: `新規登録`

#### 一覧表

表の列見出し（れつみだし: 表の上の項目名）は次のとおりです。

- `やること`
- `ジャンル`
- `優先度`
- `期限`
- `状態`
- `操作`

操作列に出すリンクは次の 2 つです。

- `編集`
- `削除`

### 6.3 検索欄・並び替えの初期値

| 項目 | 初期状態 |
|---|---|
| やること名 | 空欄 |
| ジャンル | `すべて` |
| 並び順 | `期限が近い順` |

ジャンル選択欄の選択肢は次の 6 つです。

- `すべて`
- `デザイン`
- `マーケティング`
- `プログラミング`
- `資格`
- `就職活動`

### 6.4 一覧が 0 件のとき

一覧に 1 件もないとき、または絞り込み結果が 0 件のときは、表の代わりに次の文を出します。

- `まだ1件も登録されていません`

### 6.5 画面遷移

| 操作 | 遷移先 |
|---|---|
| `新規登録` | 登録画面 |
| `編集` | 選んだ行の編集画面 |
| `削除` | 選んだ行の削除確認画面 |
| `絞り込む` | 一覧画面のまま再表示 |
| `期限が近い順` | 一覧画面のまま再表示 |
| `期限が遠い順` | 一覧画面のまま再表示 |

## 7. 登録画面

### 7.1 基本情報

| 項目 | 内容 |
|---|---|
| 画面名 | 登録画面 |
| URL | `/todos/new` |
| 役割 | 新しいやることを入力する |

### 7.2 入力項目

| 表示名 | 入力形式 | 必須 | 初期値 |
|---|---|---|---|
| やること | 1行テキスト | 必須 | 空欄 |
| メモ | 1行テキスト | 任意 | 空欄 |
| ジャンル | プルダウン（選ぶ欄） | 必須 | 空欄 |
| 優先度 | プルダウン | 必須 | 空欄 |
| 期限 | 日付入力 | 任意 | 空欄 |

ジャンルの選択肢:

- `デザイン`
- `マーケティング`
- `プログラミング`
- `資格`
- `就職活動`

優先度の選択肢:

- `高`
- `中`
- `低`

### 7.3 画面に出す言葉

- 画面タイトル: `Todo登録`
- ボタン: `確認する`
- ボタン: `一覧へ`

### 7.4 入力エラー

入力エラー（にゅうりょくエラー: 入れた内容が決まりに合っていないこと）のときは、この画面に留まり、該当欄の下に次の文を出します。

- `やることを入力してください`
- `やることは255文字以内で入力してください`
- `メモは255文字以内で入力してください`
- `ジャンルを選んでください`
- `優先度を選んでください`

### 7.5 画面遷移

| 操作 | 遷移先 |
|---|---|
| `確認する` | 登録確認画面 |
| `一覧へ` | 一覧画面 |
| 入力エラーで送信 | 登録画面に留まる |

## 8. 登録確認画面

### 8.1 基本情報

| 項目 | 内容 |
|---|---|
| 画面名 | 登録確認画面 |
| URL | `/todos/confirm` |
| 役割 | 入力内容を確認し、登録するか戻るかを選ぶ |

### 8.2 画面に出す項目

確認用に文字として並べる項目は次の 5 つです。

- `やること`
- `メモ`
- `ジャンル`
- `優先度`
- `期限`

未入力の表示:

- メモ未入力時: `（入力なし）`
- 期限未入力時: `（入力なし）`

### 8.3 画面に出す言葉

- 画面タイトル: `Todo登録確認`
- ボタン: `登録する`
- ボタン: `書き直す`

### 8.4 画面遷移

| 操作 | 遷移先 |
|---|---|
| `登録する` | 一覧画面 |
| `書き直す` | 登録画面 |

`書き直す` のときは、入力済みの値を保ったまま登録画面へ戻ります。

## 9. 編集画面

### 9.1 基本情報

| 項目 | 内容 |
|---|---|
| 画面名 | 編集画面 |
| URL | `/todos/{id}/edit` |
| 役割 | 登録済みのやることを修正する |

### 9.2 入力項目

| 表示名 | 入力形式 | 必須 | 初期値 |
|---|---|---|---|
| やること | 1行テキスト | 必須 | 現在値 |
| メモ | 1行テキスト | 任意 | 現在値 |
| ジャンル | プルダウン | 必須 | 現在値 |
| 優先度 | プルダウン | 必須 | 現在値 |
| 期限 | 日付入力 | 任意 | 現在値 |
| 完了 | チェックボックス | 任意 | 現在値 |

完了チェックの見せ方:

- チェックあり: 完了
- チェックなし: 未完了

### 9.3 画面に出す言葉

- 画面タイトル: `Todo編集`
- ラベル: `完了`
- ボタン: `確認する`
- ボタン: `一覧へ`

### 9.4 入力エラー

入力エラー時の文は登録画面と同じです。

- `やることを入力してください`
- `やることは255文字以内で入力してください`
- `メモは255文字以内で入力してください`
- `ジャンルを選んでください`
- `優先度を選んでください`

### 9.5 存在しない `id`

存在しない `id` を開いたときは専用画面を出さず、一覧画面へ戻して次の文を出します。

- `見つかりませんでした`

### 9.6 画面遷移

| 操作 | 遷移先 |
|---|---|
| `確認する` | 編集確認画面 |
| `一覧へ` | 一覧画面 |
| 入力エラーで送信 | 編集画面に留まる |
| 存在しない `id` で表示 | 一覧画面 |

## 10. 編集確認画面

### 10.1 基本情報

| 項目 | 内容 |
|---|---|
| 画面名 | 編集確認画面 |
| URL | `/todos/{id}/confirm` |
| 役割 | 修正内容を確認し、保存するか戻るかを選ぶ |

### 10.2 画面に出す項目

確認用に文字として並べる項目は次の 6 つです。

- `やること`
- `メモ`
- `ジャンル`
- `優先度`
- `期限`
- `完了`

未入力の表示:

- メモ未入力時: `（入力なし）`
- 期限未入力時: `（入力なし）`

完了の表示:

- 完了のとき: `完了`
- 未完了のとき: `未完了`

### 10.3 画面に出す言葉

- 画面タイトル: `Todo編集確認`
- ボタン: `保存する`
- ボタン: `書き直す`

### 10.4 画面遷移

| 操作 | 遷移先 |
|---|---|
| `保存する` | 一覧画面 |
| `書き直す` | 編集画面 |

`書き直す` のときは、入力済みの値を保ったまま編集画面へ戻ります。

## 11. 削除確認画面

### 11.1 基本情報

| 項目 | 内容 |
|---|---|
| 画面名 | 削除確認画面 |
| URL | `/todos/{id}/delete` |
| 役割 | 削除対象を確認し、削除するか中止するかを選ぶ |

### 11.2 画面に出す項目

表示する項目は次の 3 つです。

- `やること`
- `ジャンル`
- `期限`

期限が未入力のとき:

- `（入力なし）`

入力欄は置きません。

### 11.3 画面に出す言葉

- 画面タイトル: `Todo削除確認`
- ボタン: `消す`
- ボタン: `やめる`

### 11.4 存在しない `id`

存在しない `id` を開いたときは専用画面を出さず、一覧画面へ戻して次の文を出します。

- `見つかりませんでした`

### 11.5 画面遷移

| 操作 | 遷移先 |
|---|---|
| `消す` | 一覧画面 |
| `やめる` | 一覧画面 |
| 存在しない `id` で表示 | 一覧画面 |

## 12. 共通部品

共通部品（きょうつうぶひん: 複数画面で同じ形で使う表示の部品）は次のとおりです。

| 部品 | 使い方 |
|---|---|
| `container` | 6画面すべての外枠 |
| `page-title` | 6画面すべてのタイトル |
| `todo-table` | 一覧画面の表 |
| `flash` | 一覧画面上部の操作結果メッセージ |
| `search-bar` | 一覧画面の検索・並び替えの帯 |
| `form-row` | 登録画面・編集画面の1項目分 |
| `form-label` | 登録画面・編集画面のラベル |
| `form-input` | 登録画面・編集画面の入力欄・選択欄 |
| `error` | 登録画面・編集画面のエラーメッセージ |
| `confirm-value` | 登録確認・編集確認・削除確認の表示値 |
| `confirm-actions` | 確認画面のボタン並び |
| `btn` | 主なボタン |
| `btn sub` | 戻る系のボタン |
| `link` | 一覧画面のリンク |

## 13. 補足

- 画面に出す言葉は、利用者が見て分かりやすい日本語に統一します。
- 画面数は 6 画面で固定し、0件専用画面やエラー専用画面は増やしません。
- 絞り込み条件と並び順は URL の後ろに付けて保持します。
